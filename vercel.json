{
  "buildCommand": "npm run build",
  "outputDirectory": "apps/recorder-pwa/dist",
  "installCommand": "npm install",
  "functions": {
    "api/ingest.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
