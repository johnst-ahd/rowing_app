# Vercel / local — ingest API auth (optional; leave empty to disable auth in dev)
INGEST_TOKEN=your-secret-token-here

# Example Traccar OsmAnd endpoint (configure in PWA Settings UI per device)
# TRACCAR_OSMAND=https://your-traccar-server.com:5055
